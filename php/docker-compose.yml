services:
  php:
    image: localhost:5000/php:dev
    build: . 
    ports:
      - 8080:80
    volumes:
      - ./app:/var/www/html
    restart: always
  
  redis:
    image: reg.ntl.nc/library/redis:latest
